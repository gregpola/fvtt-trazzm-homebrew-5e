/*

*/
const optionName = "Nightfang";
const version = "13.5.0";

try {
    if (args[0].macroPass === "postActiveEffects") {
    }

} catch (err) {
    console.error(`${optionName}: ${version}`, err);
}
